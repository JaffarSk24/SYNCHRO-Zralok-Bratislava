# Copy to .env.local and fill in — .env.local is gitignored and never committed.

# Instagram public app ID (used by fetch-instagram.mjs via curl)
IG_APP_ID=936619743392459

# Production site URL (used for canonical/og tags)
SITE_URL=https://synchrozralok.com
